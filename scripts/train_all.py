"""Backward-compatible entry point for the reproducible k sweep.

Use ``run_k_experiment.py`` directly when custom arguments are required.
"""

from run_k_experiment import main


if __name__ == "__main__":
    main()
