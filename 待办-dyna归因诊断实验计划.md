# 【待办】Dyna 机制归因诊断实验计划

> 创建日期：2026-08-05
> 状态：**待办——阶段 E 正式训练完成后启动（Phase 0 可立即开始，不占 GPU）**
> 目标：回答"Dyna-Q 的样本效率收益（若存在）到底来自哪里"，为算法重构方向
> （不确定性感知规划 vs 上层模型驱动 vs 放弃下层规划）提供决策依据。

---

## 1. 背景与动机

阶段 B-E 之后，唯一存活的主张是"Dyna 提升样本效率"（4/5 seed 方向一致，
p=0.3125 不显著）。在投入算法重构或 Sionna 之前，必须先归因。

### 代码事实（2026-08-05 审计确认）

1. `dyna_plan`（`src/hierarchical_agent.py:488-554`）从 replay buffer 采样
   **真实经历过的 (s,a)**，用学习模型预测 (r̂, ŝ′) 做 1 次单样本 Q 更新。
   真实 (r, s′) 存在同一条 tuple 中——模型预测相对存储真值**只可能引入误差
   或平滑**，不提供任何新状态覆盖（当前实现不做新状态探索）。
2. 每个环境步、每个 agent 的更新次数：
   - NoDyna：1 次 batch-32 Q 更新；
   - DynaQ-K1：1 次 batch-32 Q 更新 + 1 次模型更新 + 1 次单样本模型预测 Q 更新。
   **额外梯度更新本身是未被控制的混杂变量。**
3. 下层世界模型的 next-state 依赖上层 UAV 移动（上层策略在训练中演化），
   模型在追一个非平稳目标；模型损失 22±15 中多少来自信道随机性/非平稳性，
   多少来自欠拟合，未知。

### 数据矛盾（必须先解决）

阶段 B 报告 DynaQ 收敛 330±169 轮（vs NoDyna 525）；阶段 D 的 K=1 同指标为
1282±428 轮。二者相差约 4 倍。可能原因：代码版本、K 值、warm-up、收敛指标
实现差异。**未解释此矛盾前，"1.59× 加速"不得再被引用。**

## 2. 竞争假设

| 假设 | 内容 | 若成立的出路 |
|---|---|---|
| H-updates | 加速来自额外梯度更新，与模型无关 | 撤回 Dyna 主张；转上层模型驱动或设计定位 |
| H-smoothing | 模型平滑了随机信道的目标值（去噪），真实有益 | 强化模型主线 → 不确定性感知规划 → Sionna 放大器 |
| H-error | 模型误差有害，Dyna 弱于平凡对照 | 不确定性/ensemble 方向，动机由本实验直接提供 |
| H-null | K=1 单样本更新影响可忽略，历史差异是噪声/混杂 | 阶段 B 加速结论作废；转向 H2 路线（上层） |

## 3. 实验臂设计

三臂对照，唯一差异是"额外那 1 次单样本更新用什么目标值"：

| 臂 | 每步下层更新 | 额外更新的 (r, s′) 来源 | 模型训练 | 状态 |
|---|---|---|---|---|
| NoDyna | 1×batch32 | 无额外更新 | 无 | 基线 |
| **Replay-1**（新） | 1×batch32 + 1×单样本 | **buffer 存储的真实值**（= 完美 Oracle） | 无 | 需实现 |
| DynaQ-1 | 1×batch32 + 1×单样本 | 学习模型预测值 | 有 | 已有代码 |

实现要求：

- 在 `HierarchicalAgent` 增加 `planning_mode ∈ {'model', 'replay'}` 参数，
  `'replay'` 时 `dyna_plan` 跳过 `model_predict`，直接使用存储的 (r, ns)；
  采样、门控（`max(k, warmup)`）、更新形式与 `'model'` 完全一致；
- 默认值保持 `'model'`，不改变任何现有行为（红线：不动物理公式）；
- **零成本仪表**：`'model'` 模式下顺手记录每个规划样本的
  |r̂−r| 与 ‖ŝ′−s′‖（真值就在 tuple 里），按 episode 聚合输出——
  得到"规划所用样本上的在线模型误差曲线"，用于误差-性能相关分析。

## 4. 阶段划分

### Phase 0：零 GPU 预备（可立即开始，约 1-2 天）

- [ ] **0a 矛盾审计**：核对阶段 B DynaQ 的 K 值、warm-up、commit 与收敛指标实现，
      解释 330 vs 1282 的差异；输出结论"哪些历史数字仍可引用"。
- [ ] **0b 可比性审计**：确认阶段 B NoDyna 5-seed 与阶段 D K=1 5-seed 是否同
      commit 同口径可直接配对；不可比则将 NoDyna 重跑纳入 Phase 2。
- [ ] **0c 前瞻性度量重分析**：对全部现有 5-seed 数据计算
      "达到固定 Ξ 阈值所需 episode 数"（阈值预注册为：阶段 B NoDyna 5-seed
      最终 Ξ 均值的 90%，即 0.9×0.0172≈0.0155，用 100 轮滚动 Ξ 首达点）；
      替换回顾性 90% 峰值指标作为本诊断的主指标。
- [ ] **0d 离线模型误差分解**：加载阶段 D K=1 checkpoint，冻结策略采集若干
      episode 转移，将模型 state 误差按维度组分解（buffer/energy 确定性维 vs
      信道随机维 vs 位置维，按各维标准差归一化）。回答：误差是集中在本就
      不可预测的维度（模型≈贝叶斯最优，支持 H-smoothing），还是确定性维也
      学不准（欠拟合，支持 H-error）。
- [ ] **0e 实现 Replay-1 + 在线误差仪表**，1-episode 冒烟测试通过。

### Phase 1：正式对照（GPU，阶段 E 结束后）

- 配置：Case 1、`paper_xi`、K=1、warm-up=32（与阶段 D K=1 数据配对）、
  seeds 42/123/2026/3407/8888、早停规则同阶段 B/D、GPU1、3 workers；
- 必跑：Replay-1 × 5 seeds（约 15-19h/run，3 workers ≈ 2 天墙钟）；
- 条件跑：NoDyna × 5 seeds（仅当 0b 判定不可比，+约 1.5-2 天）；
- DynaQ-1 × 5 seeds 复用阶段 D 数据（若 0b 通过）；
- 训练期间不修改服务器 `src/`、`scripts/`；记录 commit/seed/结果映射清单。

### Phase 2：联合分析与判定

主指标：episodes-to-threshold（0c 定义）；
次指标：最终 Ξ（末 50 轮）、末窗系统指标（交付/接收/能耗/碰撞）、墙钟、
在线模型误差曲线。5-seed 精确置换检验照常报告，但**判定以方向一致性
（≥4/5）+ 效应量为准**——这是内部决策实验，不是发表统计。

## 5. 预注册判定矩阵

| 观测结果（episodes-to-threshold） | 结论 | 后续行动 |
|---|---|---|
| Replay-1 ≈ DynaQ-1，均快于 NoDyna | H-updates：收益=额外更新 | 撤回"模型规划"表述；论文改为计算匹配对照；重构转上层/设计定位 |
| DynaQ-1 快于 Replay-1（≥4/5 同向） | H-smoothing：模型去噪真实有益 | 立项不确定性感知自适应规划；Sionna 作为展示环境 |
| DynaQ-1 慢于 Replay-1（≥4/5 同向） | H-error：模型误差有害 | 同上立项，且本结果直接构成动机章节；现论文须披露该对照 |
| 三臂无可分辨差异 | H-null | 阶段 B 加速为噪声/混杂；Dyna 主线关闭，启动 H2 路线评估 |

判定歧义时（如 3/5 方向、效应 <10%）：默认按"证据不足以支持模型收益"处理，
不追加 seed 硬凑显著性。

## 6. 成本预算

| 项 | GPU | 墙钟 |
|---|---|---|
| Phase 0 | 无 | 1-2 天（本地/CPU） |
| Replay-1 ×5 | GPU1 | ≈2 天 |
| NoDyna ×5（条件） | GPU1 | ≈1.5-2 天 |
| 分析报告 | 无 | 0.5 天 |
| **合计** | | **3.5-6 天** |

可选延伸（仅当 H-smoothing/H-error 成立后考虑）：Replay-5 vs 阶段 D K=5
数据，检验误差复合随 K 放大的机制——先不排期。

## 7. 红线与约束

- 不修改 `src/system_model.py` 物理公式；
- `planning_mode` 默认 `'model'`，所有历史行为可精确复现；
- 阶段 E 训练完成前不占用 GPU1；
- 原始 JSON/checkpoint 只增不删；
- 本诊断结果单独成报告，不与阶段 B/D 已发布数字混合表述。

## 8. 交付物

1. `reports/dyna归因诊断报告_YYYYMMDD.md`：四假设判定 + 证据链；
2. 阶段 B/D 收敛数字矛盾的解释备忘（0a）；
3. 前瞻性度量下的全算法收敛表（0c，可直接进论文防御早停质疑）；
4. 模型误差分解图（0d + 在线仪表）；
5. 更新 `会话交接总结` 与 `training_and_wcl_plan.md` 的阶段表。
