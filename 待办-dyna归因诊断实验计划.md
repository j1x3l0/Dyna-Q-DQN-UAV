# 【待办】Dyna 机制归因诊断实验计划

> 创建日期：2026-08-05
> 状态：**进行中——Phase 0a/0c/0d/0e 已完成（2026-08-05，见下方结论）；0b 待做；
> GPU 部分（Phase 1）待阶段 E 正式训练完成后启动**
> 目标：回答"Dyna-Q 的样本效率收益（若存在）到底来自哪里"，为算法重构方向
> （不确定性感知规划 vs 上层模型驱动 vs 放弃下层规划）提供决策依据。

---

## 1. 背景与动机

阶段 B-E 之后，唯一存活的主张是"Dyna 提升样本效率"（4/5 seed 方向一致，
p=0.3125 不显著）。在投入算法重构或 Sionna 之前，必须先归因。

### 代码事实（2026-08-05 审计确认）

1. `dyna_plan`（`src/hierarchical_agent.py:488-554`）从 replay buffer 采样
   **真实经历过的 (s,a)**，用学习模型预测 (r̂, ŝ′) 做 1 次单样本 Q 更新。
   真实 (r, s′) 存在同一条 tuple 中——模型预测相对存储真值**只可能引入误差
   或平滑**，不提供任何新状态覆盖（当前实现不做新状态探索）。
2. 每个环境步、每个 agent 的更新次数：
   - NoDyna：1 次 batch-32 Q 更新；
   - DynaQ-K1：1 次 batch-32 Q 更新 + 1 次模型更新 + 1 次单样本模型预测 Q 更新。
   **额外梯度更新本身是未被控制的混杂变量。**
3. 下层世界模型的 next-state 依赖上层 UAV 移动（上层策略在训练中演化），
   模型在追一个非平稳目标；模型损失 22±15 中多少来自信道随机性/非平稳性，
   多少来自欠拟合，未知。

### 数据矛盾（已解决，见 Phase 0a 结论）

阶段 B 报告 DynaQ 收敛 330±169 轮（vs NoDyna 525）；阶段 D 的 K=1 同指标为
1282±428 轮。二者相差约 4 倍。

**Phase 0a 审计结论（2026-08-05，备忘 `reports/阶段B_D收敛矛盾审计备忘_20260805.md`）**：
不是指标 bug、不是 K 或奖励差异、也不是 warm-up——而是两个训练 commit 之间
`dyna_plan` 的真实代码重写（批量+clip_grad_norm → 逐样本+无 clip）。K=1 时
唯一功能性差异 = **DQN 规划梯度的 clip_grad_norm**（阶段 B 有、阶段 D 无）。
本地 git 历史与服务器分叉：阶段 B 真实训练 commit=服务器 `264c2d7`，阶段 D=`bd067e0`。
**推论：NoDyna 代码两 commit 间逐字节一致，阶段 B 的 NoDyna（525）对当前代码仍有效；
当前代码 DynaQ ≈1282 → "1.59× 加速"不成立、方向可能反转（DynaQ 反而更慢）。**
**"1.59× 加速"不得再被引用，直到当前代码下配对复测（Phase 1）。**

## 2. 竞争假设

| 假设 | 内容 | 若成立的出路 |
|---|---|---|
| H-updates | 加速来自额外梯度更新，与模型无关 | 撤回 Dyna 主张；转上层模型驱动或设计定位 |
| H-smoothing | 模型平滑了随机信道的目标值（去噪），真实有益 | 强化模型主线 → 不确定性感知规划 → Sionna 放大器 |
| H-error | 模型误差有害，Dyna 弱于平凡对照 | 不确定性/ensemble 方向，动机由本实验直接提供 |
| H-null | K=1 单样本更新影响可忽略，历史差异是噪声/混杂 | 阶段 B 加速结论作废；转向 H2 路线（上层） |

## 3. 实验臂设计

三臂对照，唯一差异是"额外那 1 次单样本更新用什么目标值"：

| 臂 | 每步下层更新 | 额外更新的 (r, s′) 来源 | 模型训练 | 状态 |
|---|---|---|---|---|
| NoDyna | 1×batch32 | 无额外更新 | 无 | 基线 |
| **Replay-1**（新） | 1×batch32 + 1×单样本 | **buffer 存储的真实值**（= 完美 Oracle） | 无 | 需实现 |
| DynaQ-1 | 1×batch32 + 1×单样本 | 学习模型预测值 | 有 | 已有代码 |

**Phase 0a 新增建议（机制归因候选，不与三臂冲突）**：三臂回答"额外更新的目标值来源"；
对"clip_grad_norm 移除是否就是历史加速反转的原因"这一假设，建议**第 4 臂 Clip-1 =
DynaQ-1 + 恢复 clip_grad_norm(DQN, 1.0)**（即阶段 B 旧版 dyna_plan 行为），与 DynaQ-1
对照可直接验证该假设。Clip-1 作为条件加跑（+1 组 ×5 seeds ≈ +2 天），Phase 0a 假说
需闭环时启动。

实现要求：

- 在 `HierarchicalAgent` 增加 `planning_mode ∈ {'model', 'replay'}` 参数，
  `'replay'` 时 `dyna_plan` 跳过 `model_predict`，直接使用存储的 (r, ns)；
  采样、门控（`max(k, warmup)`）、更新形式与 `'model'` 完全一致；
- 默认值保持 `'model'`，不改变任何现有行为（红线：不动物理公式）；
- **零成本仪表**：`'model'` 模式下顺手记录每个规划样本的
  |r̂−r| 与 ‖ŝ′−s′‖（真值就在 tuple 里），按 episode 聚合输出——
  得到"规划所用样本上的在线模型误差曲线"，用于误差-性能相关分析。

## 4. 阶段划分

### Phase 0：零 GPU 预备（0a 已完成；0b-0e 待做，约 1-2 天）

- [x] **0a 矛盾审计**（✅ 2026-08-05）：K=1、paper_xi 两阶段一致；收敛指标逐字节等价；
      warm-up 门控等价（非归因）；真因 = dyna_plan 重写（clip_grad_norm 移除）。
      结论见 `reports/阶段B_D收敛矛盾审计备忘_20260805.md`。
      **哪些历史数字仍可引用**：阶段 B 最终性能 ✅；阶段 B 内部 4/5 同向趋势 ✅（须注明
      旧版 clip 代码）；阶段 D K=1 vs K=5 相对结论 ✅。**"1.59× 加速" ❌；330/1282 绝对
      收敛数字 ❌；warm-up 归因 ❌。**
- [ ] **0b 可比性审计**：阶段 B NoDyna（264c2d7）与阶段 D DynaQ（bd067e0）非同 commit，
      不可直接配对；但 NoDyna 代码两 commit 间逐字节一致、环境/训练循环未变，阶段 B
      NoDyna 数字对当前代码仍具代表性（仅作方向性参考，不作配对统计）。
      **结论：NoDyna ×5 重跑纳入 Phase 1 必跑**（需当前代码下的干净配对对照）。
- [x] **0c 前瞻性度量重分析**（✅ 2026-08-05）："达到固定 Ξ 阈值所需 episode 数"
      （阈值预注册 = 0.9×阶段 B NoDyna 最终 Ξ 均值 ≈ **0.01549**，100 轮滚动 Ξ 首达点）。
      结果表 `results/prospective_metric_20260805.json`；本度量替代回顾性 90% 峰值
      指标作为诊断主指标。

      **结果（episodes-to-threshold，5-seed）**：

      | 算法 | 达标 | 均值(达标者) | 逐 seed |
      |---|---|---|---|
      | B-NoDyna（旧代码） | 5/5 | 130 | 108/100/104/181/156 |
      | B-DynaQ（旧代码） | 4/5 | 126 | 105/100/199/100/**never(8888)** |
      | B-MADDPG | 2/5 | 262 | — |
      | D-DynaQ K=1（当前代码） | 5/5 | 379 | 498/222/392/468/316 |
      | D-DynaQ K=5（当前代码） | 5/5 | 561 | — |

      **关键结论**：①阶段 B 内部，前瞻性度量下 **DynaQ ≈ NoDyna**（达标者均值 126 vs 130），
      且 DynaQ seed8888 **永不达标**（最终 Ξ=0.0123 < 阈值）——回顾性"1.59× 加速"
      **不复现**，为回顾性峰值指标（瞬时峰值形状）伪象；②阈值升高到 0.017/0.018 时
      DynaQ 在达标 seed 上更快（140 vs 244 等），但 8888 持续不达标，**加速对阈值与
      never-seed 处理敏感，不稳健**；③当前代码 DynaQ K=1（379）比阶段 B NoDyna（130）
      慢 ~3×，与 0a"方向可能反转"一致；④MADDPG 仅 2/5 达标，确实弱。
      **注意**：每 episode Ξ 高度噪声（早期探索尖峰），绝对 episode 数含尖峰累积成分；
      相对比较（同阈值下算法间）仍有效。
- [x] **0d 离线模型误差分解**（✅ 2026-08-05）：加载阶段 D K=1 checkpoint
      （seed123 final_ep2779 + seed42 final_ep2833），冻结策略（epsilon=0）采集
      转移，模型 state 误差按维度组分解、按各维 std 归一化。脚本
      `scripts/analyze_model_error_decomposition.py`。**结果（归一化误差
      =|err|/dim-std；<1 好、~1=预测均值、>1 差，两 seed 一致）**：

      | 维度组 | seed123 | seed42 |
      |---|---|---|
      | UAV 位置 (det) | 0.004 | 0.001 |
      | UAV buffer/energy (det) | 0.005 | 0.002 |
      | d_i0 (det) | 0.013 | 0.001 |
      | RBS 信道 (stoch) | **2978** | **1177** |
      | GU buffer/energy (det) | 0.041 | 0.006 |
      | GU 信道 (stoch) | **4693** | **4362** |
      | reward 预测 \|r̂−r\| | 0.56 | 0.62 |

      **结论：指向 H-error，否定 H-smoothing。** ①确定性维（pos/buffer/energy/
      d_i0/GU det）模型学得非常好（归一化误差 0.001-0.041）；②信道维（RBS+GU）
      模型**灾难性错标定**——真实信道 ~5e-5（std 3e-5），模型输出 ~±0.02-0.06
      （**大 ~1000×**），归一化误差 1177-4693，**远差于预测均值（应为 ~1）**。
      原因：MSE 损失被量级更大的确定性维主导，信道维 ~5e-5 的微小目标产生极小
      梯度 → 输出层权重未收敛到正确尺度。③模型看不到 upper action（输入仅 2M 维
      lower action），位置变化结构上不可预测，只能靠"一步内位置近似不变"。
      ④reward 预测误差 0.56 ≈ 典型非零 \|r\|(=2.15) 的 13%，尚可。
      **推论**：dyna_plan 把 ŝ′ 喂给 target DQN 时，信道维 ~1000× 超出分布 →
      规划目标被扭曲状态污染，是"模型误差有害"的具体机制；模型并非"信道去噪
      （平滑）"。净效果待 Phase 1（Replay-1 vs DynaQ-1）量化。
- [x] **0e 实现 Replay-1 + 在线误差仪表**（✅ 2026-08-05）：
      `src/hierarchical_agent.py` 增加 `planning_mode ∈ {'model','replay'}`（构造参数或
      `config.planning_mode`，默认 `'model'`，不改变现有行为）；`'replay'` 时 `dyna_plan`
      直接用 tuple 存储的真实 (r, ns)（= 完美 Oracle），采样/门控/更新形式与 `'model'` 完全
      一致；`'model'` 时零成本记录每个规划样本的 |r̂−r| 与 ‖ŝ′−s′‖，按 agent 聚合
      （`planning_error_summary()` / `reset_planning_errors()`）。
      冒烟测试通过：`scripts/test_replay_1_smoke.py`（model 记录 200 样本/agent、
      replay 零记录、非法模式报错）。
      **设计决定**：Replay-1 **保留模型训练**（update_model 照常），与 DynaQ-1 的唯一
      差异 = 规划目标值来源；否则 Replay-1 会同时少掉"模型训练"这一差异，破坏对照干净性。

### Phase 1：正式对照（GPU，阶段 E 结束后）

- 配置：Case 1、`paper_xi`、K=1、warm-up=32（与阶段 D K=1 数据配对）、
  seeds 42/123/2026/3407/8888、早停规则同阶段 B/D、GPU1、3 workers；
- 必跑：Replay-1 × 5 seeds（约 15-19h/run，3 workers ≈ 2 天墙钟）；
- **必跑：NoDyna × 5 seeds**（Phase 0a 结论：必须当前代码下配对对照，+约 1.5-2 天）；
- DynaQ-1 × 5 seeds 复用阶段 D 数据（阶段 D 即当前代码 bd067e0，可直接复用）；
- 条件加跑：Clip-1 × 5 seeds（验证 clip_grad_norm 假设，+约 2 天，Phase 0a 假说需闭环时）；
- 训练期间不修改服务器 `src/`、`scripts/`；记录 commit/seed/结果映射清单。

### Phase 2：联合分析与判定

主指标：episodes-to-threshold（0c 定义）；
次指标：最终 Ξ（末 50 轮）、末窗系统指标（交付/接收/能耗/碰撞）、墙钟、
在线模型误差曲线。5-seed 精确置换检验照常报告，但**判定以方向一致性
（≥4/5）+ 效应量为准**——这是内部决策实验，不是发表统计。

## 5. 预注册判定矩阵

| 观测结果（episodes-to-threshold） | 结论 | 后续行动 |
|---|---|---|
| Replay-1 ≈ DynaQ-1，均快于 NoDyna | H-updates：收益=额外更新 | 撤回"模型规划"表述；论文改为计算匹配对照；重构转上层/设计定位 |
| DynaQ-1 快于 Replay-1（≥4/5 同向） | H-smoothing：模型去噪真实有益 | 立项不确定性感知自适应规划；Sionna 作为展示环境 |
| DynaQ-1 慢于 Replay-1（≥4/5 同向） | H-error：模型误差有害 | 同上立项，且本结果直接构成动机章节；现论文须披露该对照 |
| 三臂无可分辨差异 | H-null | 阶段 B 加速为噪声/混杂；Dyna 主线关闭，启动 H2 路线评估 |

判定歧义时（如 3/5 方向、效应 <10%）：默认按"证据不足以支持模型收益"处理，
不追加 seed 硬凑显著性。

## 6. 成本预算

| 项 | GPU | 墙钟 |
|---|---|---|
| Phase 0 | 无 | 1-2 天（本地/CPU） |
| Replay-1 ×5 | GPU1 | ≈2 天 |
| NoDyna ×5（条件） | GPU1 | ≈1.5-2 天 |
| 分析报告 | 无 | 0.5 天 |
| **合计** | | **3.5-6 天** |

可选延伸（仅当 H-smoothing/H-error 成立后考虑）：Replay-5 vs 阶段 D K=5
数据，检验误差复合随 K 放大的机制——先不排期。

## 7. 红线与约束

- 不修改 `src/system_model.py` 物理公式；
- `planning_mode` 默认 `'model'`，所有历史行为可精确复现；
- 阶段 E 训练完成前不占用 GPU1；
- 原始 JSON/checkpoint 只增不删；
- 本诊断结果单独成报告，不与阶段 B/D 已发布数字混合表述。

## 8. 交付物

1. `reports/dyna归因诊断报告_YYYYMMDD.md`：四假设判定 + 证据链；
2. 阶段 B/D 收敛数字矛盾的解释备忘（0a）——✅ 已完成：
   `reports/阶段B_D收敛矛盾审计备忘_20260805.md`；
3. 前瞻性度量下的全算法收敛表（0c，可直接进论文防御早停质疑）；
4. 模型误差分解图（0d + 在线仪表）；
5. 更新 `会话交接总结` 与 `training_and_wcl_plan.md` 的阶段表。
