# 分层智能体训练与 IEEE WCL 投稿计划

> 更新日期：2026-07-26  
> 目标：完成分层智能体的可靠训练验证，并形成面向 *IEEE Wireless Communications Letters*（WCL）的可投稿证据链。

## 1. 当前状态

### 已完成的六算法训练

已完成 iDDPG、MADDPG、MATD3、CoP-MADDPG、Hier-NoDyna、Hier-DynaQ 的 seed 42 与 123 正式训练。12 次运行均正常完成或按提前停止规则结束，未发现 Python Traceback、NaN 或 CUDA OOM。

当前聚合结果如下：

| 算法 | 最终奖励（均值 ± 标准差） | 收敛轮数 | 前 500 轮奖励 |
|---|---:|---:|---:|
| Hier-DynaQ | **1594.89 ± 251.93** | **516 ± 369** | **1482.13 ± 169.76** |
| Hier-NoDyna | 1588.05 ± 188.16 | 810 ± 195 | 1433.56 ± 332.99 |
| MADDPG | 1359.21 ± 1007.92 | 1066 ± 742 | 1087.14 ± 374.79 |
| iDDPG | 1268.94 ± 465.53 | 822 ± 717 | 1276.21 ± 13.75 |
| CoP-MADDPG | 659.48 ± 401.19 | 1432 ± 1818 | 788.29 ± 503.14 |
| MATD3 | 496.67 ± 11.42 | 1494 ± 1470 | 634.35 ± 0.82 |

当前结果支持“分层架构是主要性能增益来源，Dyna-Q 主要提高样本效率”的判断：

- 两种分层算法的最终奖励均比 MADDPG 高约 17%；
- Dyna-Q 与 NoDyna 的最终奖励仅相差约 0.4%，当前不能视为显著提升；
- Dyna-Q 达到 90% 峰值奖励约需 516 轮，NoDyna 约需 810 轮，前者约快 1.57 倍。

### 分层算法修复与验证

- 已修复 `scripts/training_utils.py` 中错误的三参数 `hasattr()` 调用；该错误曾导致 NoDyna 与 Dyna-Q 在启动后立即失败。
- 已完成 NoDyna 的正式长训：seed 42、123 分别在 4436、5350 轮提前收敛。
- 已完成 Dyna-Q 的正式长训：seed 42、123 分别在 3211、2670 轮提前收敛。
- 已优化 `src/hierarchical_agent.py` 的 Dyna 规划：
  - replay buffer 未达到一个完整 batch（32 条）前，不使用未训练世界模型生成虚拟经验；
  - 对 `k` 条虚拟经验进行批量 GPU 推理和 DQN 更新，消除逐样本 CPU/GPU 往返。

### 结果归档

服务器结果位于：

```text
/mnt/UAV/results/benchmark_report_20260724_101608.json  # 四种非分层基线
/mnt/UAV/results/benchmark_report_20260725_183327.json  # NoDyna 与 Dyna-Q
```

本地实验记录：

```text
reports/六算法统一对比实验报告_20260725.md
```

## 2. 当前实验的判定标准

本轮长训判定结果：

1. 六种算法、12 个任务均正常完成或按早停结束，无 Python 异常、NaN 或 CUDA 错误。
2. 服务器已生成 JSON、文本报告、奖励对比图、叠加图和样本效率图。
3. 报告已覆盖最终奖励、收敛回合、吞吐、能耗、碰撞次数和训练时长。
4. Dyna-Q 的样本效率优势已出现，但最终奖励优势不显著。

仍未满足正式论文证据要求：每种算法只有两个 seed，且四种非分层基线和两种分层算法来自两批运行。正式统计前应在同一代码提交下补充统一实验。

## 3. 后续实验计划

### 阶段 A：六算法首轮正式训练（已完成）

| 算法 | Seed | 状态 | 目的 |
|---|---:|---|---|
| iDDPG / MADDPG / MATD3 / CoP-MADDPG | 42、123 | 已完成 | 非分层算法基线 |
| NoDyna | 42、123 | 已完成 | 分层、无模型规划基线 |
| Dyna-Q | 42、123 | 已完成 | 验证模型规划收益 |

阶段 A 形成趋势结论，但不作为最终论文统计结论。主要原因是每种算法只有两个 seed，且 MADDPG、iDDPG 和 CoP-MADDPG 的跨 seed 方差较大。

### 阶段 B：统一核心三算法并补齐统计效力（已完成）

在冻结代码提交、环境参数和奖励定义后，统一运行 MADDPG、NoDyna、Dyna-Q：

| 算法 | 计划 seeds | 必要性 |
|---|---|---|
| MADDPG | 42、123、2026、3407、8888 | 验证分层架构相对单层基线的收益 |
| NoDyna | 42、123、2026、3407、8888 | 隔离分层架构本身的贡献 |
| Dyna-Q | 42、123、2026、3407、8888 | 验证模型规划的增量贡献 |

执行要求：

1. 训练前记录 Git commit、完整参数、Python/PyTorch/CUDA 版本和 GPU 型号；
2. 三种算法采用相同场景、停止准则和评估窗口；
3. 保留每个 seed 的逐轮 reward 与系统指标，不只保留聚合报告；
4. 报告均值、标准差、95% 置信区间和单次运行结果；
5. 分别检验最终奖励、前 500 轮奖励和收敛轮数；
6. 在训练开始前确认 GPU 空闲，先短验证再启动长训。

本轮冻结配置（2026-07-26）：

- 算法：MADDPG、Hier-NoDyna、Hier-DynaQ；
- seeds：42、123、2026、3407、8888；
- 奖励：`paper_xi`，即 RBS 实际接收数据 / UAV 总能耗；
- 上限：每次 8000 episodes，统一采用 patience=1500、window=500 的提前停止；
- 场景：case 1；设备：GPU1；
- 输出：逐 episode reward、吞吐、RBS 转发量、能耗、碰撞和论文 Ξ，并使用
  `算法_paper_xi_seed` 命名 checkpoint，避免覆盖历史 `ee_ratio` 模型。

阶段 B 结果（2026-07-29）：

- 15/15 次运行完成，失败 0；MADDPG、NoDyna、DynaQ 各 5 个配对 seeds；
- 按每个 seed 真实停止点计算，最终奖励分别为 293.72 ± 178.19、
  515.02 ± 42.32、512.75 ± 105.24；
- DynaQ 与 NoDyna 的最终奖励和论文 Ξ 基本相同；
- DynaQ 平均收敛轮数为 330.4，NoDyna 为 524.6，呈 1.59× 描述性加速趋势；
- 采用 32 种配对符号翻转的精确置换检验并对三个主假设做 Holm 校正后，所有主检验
  均未达到 α=0.05；DynaQ 收敛加速的 Holm p=0.3125；
- 5 seeds 的双侧精确检验最小 p=0.0625，统计分辨率不足。投稿不得声称“显著加速”；
- 详细报告见 `reports/阶段B五种子显著性检验报告_20260729.md`，机器可读结果见
  `results/stage_b_significance_20260729.json`。

### 阶段 C：奖励与系统目标一致性检查（已完成）

当前 NoDyna 的吞吐量高于 Dyna-Q，但 Dyna-Q 的最终奖励略高。进入方法消融前，必须解释奖励与系统能效指标之间的关系：

1. [进行中] 分解每轮 reward 中的吞吐收益、能耗成本和碰撞惩罚；
2. [代码已完成，待服务器验证] 直接计算论文定义的系统能效 $\Xi$；
3. [待实验] 检查 reward 排名是否与 $\Xi$ 排名一致；
4. [待实验] 对零奖励轮次、碰撞轮次和异常高奖励轮次分别统计；
5. [持续执行] 若修改 reward，旧实验只作历史结果，不与新口径结果混合。

#### 2026-07-26 代码改进记录

已维护 `scripts/reward_decomposition.py`：

- 修复未初始化 `state_dim`、`action_dim` 导致脚本无法启动的问题；
- 将 NoDyna 纳入 MADDPG、NoDyna、Dyna-Q 核心三算法比较；
- 总能耗统一包含飞行、感知与转发能耗；
- 新增论文系统能效 `paper_xi`，按逐时隙 RBS 吞吐/总能耗计算时间平均；
- 额外保存 `paper_xi_ratio_of_sums`，用于区分平均比值与总量之比；
- 单独记录 reward 使用的能效代理，供 reward 与论文目标一致性检查；
- 修正 NoDyna 与 Dyna-Q 的图表标签，避免结果混淆。

已在 GPU1 完成 1 episode 冒烟测试和 seed 42 的 50 episode 核心三算法短测。
`paper_xi` 模式下 reward 与论文 Ξ 的相关系数均约为 0.9999997。Hier-DynaQ
的转发/接收比由历史 `ee_ratio` 下约 1.5% 提升至 54.75%，50 轮论文 Ξ 为
0.01033，末 10 轮 Ξ 为 0.01868；动作索引、NaN、CUDA OOM 与运行异常检查均通过。

阶段 C 结论：历史奖励允许“只收集、不向 RBS 交付”的代理目标偏差，`paper_xi`
能够修正该偏差。阶段 B 正式统计统一使用 `paper_xi`，旧 `ee_ratio` 仅作为消融基线。

### 阶段 D：Dyna-Q 消融实验（K sweep 已完成；执行 warm-up 门控）

| 消融维度 | 设置 | 要回答的问题 |
|---|---|---|
| Dyna-K | 1, 3, 5, 10 | 规划步数是否存在最优区间？ |
| 模型 warm-up | 0 / 32 / 64 / 128 | 未训练或低精度模型是否损害早期学习？ |
| 规划实现 | 逐样本 / 批量 | 批量化是否降低训练开销？ |
| 初始条件 | Case 1 / Case 2 | 方法对初始部署是否稳健？ |
| 系统规模 | 不同 UAV / GU 数量 | 方法能否扩展？ |

重点指标：

- 达到最终最佳滚动奖励 90% 所需的真实训练回合数；
- 最终能效、吞吐、能耗、碰撞次数；
- 训练墙钟时间与 GPU 利用率；
- 世界模型预测误差随训练过程的变化。

消融顺序：

1. 先固定 warm-up=32，比较 Dyna-K；
2. 再固定最优 Dyna-K，比较 warm-up；
3. 最后只对最优组合执行 Case 2 和系统规模扩展。

K sweep 于 2026-07-29 调整为门控验证：

- 冻结提交：`bd067e0`；
- 原 K=1/3/5/10 × 5 seeds 完整 sweep 在首批 K=1 约 100 轮时主动终止，日志和
  checkpoint 保留，未进入 K=3/5/10；
- K=1 使用阶段 B 的 5-seed 结果作为参考锚点；
- 先运行 K=3/5/10、warm-up=32、seed42、200 episodes 的门控验证；
- 仅当 Ξ/收敛 AUC、模型误差、奖励方差和墙钟成本综合优于 K=1 时，候选 K 才进入
  5-seed 正式消融；K=10 若无明确收益则取消；
- 门控日志：`/root/Dyna-Q-DQN-UAV/logs/stage_d_k_gate_gpu1_20260729.log`；
- 门控清单：`/mnt/UAV/results/dyna_ablation/stage_d_k_gate_manifest_20260729.txt`。
- 为消除阶段 B 与新 warm-up 门控代码的口径差异，已补跑 K=1、warm-up=32、
  seed42、200 episodes；日志为
  `/root/Dyna-Q-DQN-UAV/logs/stage_d_k1_gate_gpu1_20260729.log`，清单为
  `/mnt/UAV/results/dyna_ablation/stage_d_k1_gate_manifest_20260729.txt`。
- 门控结果保留 K=1 与 K=5，取消 K=3 和 K=10；正式 5-seed 消融已在 GPU1 启动，
  共 10 次运行、3 个并行 worker；
- 正式日志：`/root/Dyna-Q-DQN-UAV/logs/stage_d_k_formal_gpu1_20260729.log`；
- 正式清单：`/mnt/UAV/results/dyna_ablation/stage_d_k_formal_manifest_20260729.txt`。

正式 K sweep 结论（2026-08-03）：

- K=1/K=5、warm-up=32、Case 1、5 个配对 seeds 共 10 次运行全部正常完成并早停；
- 原始汇总：`/mnt/UAV/results/dyna_ablation/dyna_ablation_20260803_092911.json`，
  SHA256 为 `66152151c7b7fb25904fcc0e973820a0f750095a046bda4ad7fbcf8d2c754c04`；
- K=5 最终 Xi 均值较 K=1 高 6.3%，但配对精确检验 `p=0.875`，bootstrap 95% CI
  跨 0；前 500/1000 轮 AUC 也无显著改善；
- K=5 平均墙钟时间增加 105.7%（38.86 h 对 18.89 h），5/5 seeds 均更慢；其
  回顾性 90% 峰值收敛点平均推迟 93.9%；
- 因此后续固定 **K=1**。完整口径、图表与限制见
  `reports/阶段D_DynaK正式消融报告_20260803.md`，机器可读结果见
  `results/stage_d_k_ablation_20260803/`；
- 下一步仅执行 warm-up=0/64/128、K=1、seed42、200 episodes 门控；现有
  warm-up=32 的同 seed 前 200 轮作为锚点。只有出现稳定的早期 AUC/模型误差收益且
  墙钟成本可接受时，候选值才进入 5-seed 正式消融。
- warm-up 门控已于 2026-08-03 10:31（服务器时间）在 GPU1 启动，PID `52855`，
  3 个并行 worker；启动前 warm-up=0/64/128 各 5 episodes 冒烟测试全部通过；
- 门控日志：`/root/Dyna-Q-DQN-UAV/logs/stage_d_warmup_gate_gpu1_20260803.log`；
  清单：`/mnt/UAV/results/dyna_ablation/stage_d_warmup_gate_manifest_20260803.txt`。
- warm-up 门控 3/3 已完成，汇总为
  `/mnt/UAV/results/dyna_ablation/dyna_ablation_20260803_113202.json`；与 warm-up=32
  的同 seed 前 200 轮统一比较后，冻结 **K=1、warm-up=128**；
- warm-up=128 相对 32 的 200 轮 Reward AUC 提高 6.7%、末 50 轮 Xi 提高 18.1%，
  模型总损失降低约 86.5%；warm-up=64 淘汰，warm-up=0 因未成熟模型立即规划且仅有
  单 seed 偶然高回报而不采用；
- 完整报告见 `reports/阶段D_warmup门控报告_20260803.md`。该选择只属于单 seed
  工程门控，不作为统计显著性主张。

代码准备状态（2026-07-26）：

- 已新增 `scripts/run_dyna_ablation.py`，支持 Dyna-K、warm-up、seed、GPU 和
  worker 参数化 sweep；
- 已将 `dyna_warmup` 显式加入 Config，默认 32，并阻止 warm-up 完成前执行模型规划；
- 已按 episode 保存世界模型 reward/state/total loss、论文 Ξ 与系统指标；
- 不同 K/warm-up 使用独立 checkpoint 命名，避免组合之间相互覆盖；
- 阶段 B 已完成。下一步将本地消融代码同步到服务器，先做 1–5 episode 冒烟测试，
  再按上述顺序执行正式实验。

### 阶段 E：扩展基线诊断与参数门控（诊断与门控已完成；正式训练待启动）

MATD3 与 CoP-MADDPG 当前结果明显低于 MADDPG，但不能直接归因于算法能力。若论文保留这两类基线，应完成：

1. [x] 检查动作范围、探索噪声、目标网络更新和 replay 更新频率；
2. [x] 为每种算法执行独立的小规模超参数搜索；
3. [x] 确认 CoP 消息确实进入策略或价值网络并产生有效梯度；
4. [x] 只使用经过合理调优的结果进行论文比较。

2026-08-03 代码诊断确认，旧实现的训练动作边界与环境执行边界不一致，CoP 通信还存在
在线消息未用于执行、发送者 encoder 收不到策略梯度的问题。修复已通过 CPU 边界/梯度
验证和 1-episode 集成冒烟测试。诊断报告见
`reports/阶段E_扩展基线代码诊断_20260803.md`。

参数门控已于 2026-08-03 12:02–12:47（服务器时间）在 GPU1 完成：6 组（MATD3/CoP ×
default/balanced/conservative）、seed42、Case 1、`paper_xi`、200 episodes、3 并行
worker，全部正常完成，无错误、NaN 或碰撞；运行代码与本地修复版逐字节一致（md5
匹配）。原始结果：`/mnt/UAV/results/stage_e_gate/stage_e_gate_20260803_124733.json`，
机器可读分析见 `results/stage_e_gate_20260803/`，门控报告见
`reports/阶段E_参数门控报告_20260803.md`。

按"完整运行中最高 Reward AUC、Ξ 平局决断"的选择规则，MATD3 与 CoP-MADDPG 均选中
conservative 配置：actor/critic lr=1e-4、MATD3 target noise 0.1、CoP encoder lr=1e-4。
MATD3 conservative 末 50 轮 Ξ 相对默认 +102.6%（0.0071→0.0143），CoP conservative
AUC +103.8%、末 50 轮 Ξ +11.3%；两种算法低学习率同时胜出是一致信号。该选择属于单
seed 工程门控，不是统计显著性结论；修复前结果不得作为算法能力结论，也不得与修复后
结果合并。

正式 5-seed 训练脚本 `scripts/run_stage_e_formal.py` 已就绪：使用与阶段 B 相同的
seeds（42、123、2026、3407、8888）、早停规则（patience=1500、window=500）、Case 1、
`paper_xi`，并为每个变体独立命名 checkpoint（`_stagee_conservative` 后缀），同时输出
标准 benchmark 报告与记录 commit/参数/seed 映射的 manifest。脚本已通过本地 1-episode
CPU 冒烟测试；启动安排在 GPU 空闲时由用户在服务器执行（上传 + nohup 启动），训练运行
期间不修改服务器 `src/` 或 `scripts/` 代码。

### 面向非无人机背景读者的疑点清单（2026-08-03）

论文不能假设读者已经理解 UAV 辅助通信。引言、系统模型、方法图和局限性部分至少要
回答下列问题；“当前回答”描述现有项目能支持的口径，“待补证据/写作动作”不得省略。

| 非专业读者可能提出的疑点 | 当前项目应给出的直接回答 | 当前证据或缺口 | 待补证据/写作动作 |
|---|---|---|---|
| 这个系统究竟在完成什么任务？ | 地面用户 GU 产生传感数据，UAV 收集数据并转发给 RBS；目标不是单纯让 UAV 飞得更远，而是提高实际交付数据与 UAV 总能耗之比。 | `paper_xi` 已按 UAV→RBS 实际交付量计算。 | 在论文第一页给出 `GU → UAV → RBS` 数据流图和一句话任务定义。 |
| UAV 在这里是普通飞行器、空中基站还是中继？ | 本项目中的 UAV 是可移动的数据收集与中继节点，同时决定覆盖位置、GU 接入和向 RBS 的转发时机；它不是完整模拟的商用 5G 基站。 | 环境包含轨迹、接入、模式选择和 RBS 转发。 | 统一使用“mobile aerial collector/relay”，避免在未实现 NR 协议栈时称“完整 5G UAV base station”。 |
| 为什么不直接增加地面基站？ | 研究对象是地面覆盖不足、临时部署、灾害或偏远传感场景；UAV 的价值是快速形成可移动覆盖，不代表所有地区都优于固定基站。 | 当前仿真没有与新增地面基站的成本对照。 | 在引言限定适用场景，并把“UAV 与地面站部署成本比较”列为研究边界。 |
| 为什么需要多架 UAV？ | 多 UAV 可并行覆盖分散 GU，但会带来重复服务、轨迹协调、资源竞争和碰撞风险，因此需要多智能体联合决策。 | 已限制每时隙每个 GU 最多由一个 UAV 服务，并记录 UAV 间碰撞。 | 用一张示意图说明单 UAV 覆盖瓶颈和多 UAV 协调代价。 |
| UAV 每一步到底决定什么？ | 上层决定移动方向、速度和是否向 RBS 调度；下层决定 GU 是否接入及采用主动 RF 或反向散射。 | 动作已在代码中实现，但论文读者难以从向量索引理解。 | 给出动作表：物理含义、连续/离散类型、范围、执行时间尺度。 |
| 为什么同时存在主动 RF 和反向散射？ | 主动 RF 通常可提供更强传输但消耗 GU 能量；反向散射适合低功耗设备，但速率和链路条件受限，策略需要在数据量与能量之间选择。 | 当前包含两种模式、GU 能量和能量采集。 | 在系统模型中用非公式语言先解释权衡，再给速率与能量公式。 |
| 轨迹是否符合真实飞行动力学？ | 当前是离散时隙下的简化运动模型，约束速度、区域和 UAV 间距；没有完整建模加速度、姿态、转弯半径、风、起降和飞控闭环。 | 可用于算法对照，但不足以证明真实飞行可直接执行。 | 在 Limitations 明确说明；禁止使用“直接部署到真实 UAV”表述。 |
| 碰撞惩罚是否等于已经保证安全？ | 不是。当前只对 UAV 间距离施加软/硬惩罚，不包含建筑物、禁飞区、通信失联、天气和形式化避碰保证。 | 有碰撞计数，但没有安全约束证明。 | 报告碰撞率；把障碍物/禁飞区交给 Sionna 场景或后续安全约束研究，不把零碰撞等同安全认证。 |
| “能效”是不是只看 reward，可能通过不转发数据作弊？ | 正式指标 \(\Xi\) 使用送达 RBS 的数据量除以 UAV 总能耗；历史 `ee_ratio` 确实存在只收集少转发的代理目标偏差，因此正式实验改用 `paper_xi`。 | 阶段 C 已验证 reward 与 \(\Xi\) 高相关，并区分接收量与实际交付量。 | 主表同时报告 \(\Xi\)、接收量、交付量、转发/接收比和总能耗。 |
| 电池状态真的反映 UAV 续航吗？ | 目前总能耗指标包含飞行、感知和转发，但代码中的 `UAV.energy` 状态只扣除转发能耗，尚不能视为严格的电池可行性模型。 | 这是实现与物理解释之间的待审计缺口。 | 投稿前核对原论文公式并决定修复或删除电池状态主张；若修改，必须作为新环境版本重跑对应实验。 |
| Dyna-Q 是否让无人机按“虚构经验”真实飞行？ | 不会。世界模型只在训练期间预测下层离散决策的奖励和下一状态，用于额外更新 DQN；虚拟经验不会发送给真实 UAV，也不改变上层运动模型。 | `dyna_plan()` 仅更新下层 DQN，世界模型由真实 replay transition 监督训练。 | 在方法图中把“real interaction”和“model planning update”画成两条路径。 |
| 上下层是否使用同一个奖励，导致目标混乱？ | 当前实现已区分：上层以 `paper_xi` 等系统交付/能效目标加碰撞惩罚训练，下层以 GU→UAV 数据接收减能量项训练；两层相关但不完全相同。 | `step_info` 分别输出 `upper_reward` 与 `lower_reward`，训练工具单独提取下层奖励。 | 在论文中分别写出两个奖励及其作用，补充二者可能产生信用分配冲突的局限性。 |
| 样本效率提升为什么有意义，GPU 时间却可能更长？ | Dyna 试图减少昂贵的真实环境交互次数，而不是保证更少 GPU 计算；在真实 UAV 场景中飞行和测量通常比模型推演更昂贵。 | 阶段 B 显示描述性收敛加速趋势，但墙钟时间更长且统计未显著。 | 分开报告交互轮数和墙钟时间，禁止把样本效率写成计算效率。 |
| 仿真中的 Rician 信道能代表真实城市 5G 吗？ | 只能代表具有较强 LoS 的简化场景，不能覆盖建筑遮挡、空间相关性、切换和复杂干扰。 | 当前主实验内部比较公平，但外部有效性有限。 | 保留 Rician 主实验，用 Case 2 和 Sionna LoS/城市遮挡做独立泛化验证。 |
| 训练好的策略换地图、用户数量或初始位置还能用吗？ | 目前只能对训练过的规模和主要场景作结论，不能声称普遍泛化。 | Case 2、系统规模和 Sionna 验证尚未全部完成。 | 报告测试范围；未测试的规模、地图和用户移动性明确列为 future work。 |
| 仿真结果能否直接说明真实无人机有效？ | 不能。当前结果证明的是受控仿真条件下的算法比较，不包含硬件时延、定位误差、传感噪声、链路中断和飞控执行误差。 | 尚无硬件在环、外场测试或真实信道测量。 | 论文使用“simulation evidence”，把硬件在环/外场验证列为后续工作。 |
| 基线表现差是否只是没有调好？ | 有这种可能，尤其 MATD3 与 CoP-MADDPG；只有统一代码、合理调参、相同 seeds 和相同停止规则的比较才可信。 | 核心三算法已统一 5 seeds；扩展基线仍需诊断。 | 主结论优先依赖 MADDPG、NoDyna、DynaQ；未经合理调优的扩展基线不得用于贬低算法。 |
| 5G-A 已提供 UAV 接入和通感，项目还有什么作用？ | 5G-A 提供连接、测量和感知能力，本项目决定如何利用这些状态进行轨迹、调度和接入联合决策；标准能力与优化策略不是替代关系。 | 已有 3GPP Release 18 定位结论，尚未实现完整 NR 协议栈。 | 把项目表述为“面向 5G/5G-A 场景的决策方法”，不表述为新 5G 标准或网络架构。 |

面向非专业读者的最低写作交付物：

1. 一张不含神经网络细节的系统图，标出 GU、UAV、RBS、两跳数据流与两类无线链路；
2. 一张上下层决策与 Dyna 虚拟更新图，明确虚拟经验只发生在训练阶段；
3. 一张符号—物理含义表，至少解释 GU、RBS、RF、backscatter、\(\Xi\)、LoS/NLoS；
4. 一个“适用场景与不适用场景”段落，说明该方法针对临时/弱覆盖传感网络，而非所有
   城市蜂窝网络；
5. 一个实现一致性审计表，逐项核对动作约束、能耗记账、奖励、碰撞、信道和停止规则；
6. 结果部分先回答“交付了多少数据、用了多少能量、是否碰撞”，再讨论总 reward；
7. Limitations 中明确列出简化动力学、解析信道、无硬件验证和有限规模泛化。

### 阶段 F：Sionna 射线追踪信道泛化（待办，可选加分项）

#### 5G/5G-A 发展影响与项目定位结论（2026-08-03）

结论：5G/5G-A 的发展不会直接取代本项目。3GPP Release 18 对 UAV 的增强主要提供
空中接入、身份识别、移动性测量、切换、干扰管理、定位及通感能力；本项目研究的是
在动态信道、能量、缓冲区和用户需求条件下，如何联合决定 UAV 轨迹、传输调度、GU
模式与接入。网络能力增强后，这一序贯联合决策问题仍然存在，并可使用更真实、更丰富
的网络测量作为状态输入。

需要避免的过时或高风险表述：

- 不以“5G 无人机通信架构”或“分层 MARL 架构”作为核心创新；
- 不声称解析 Rician、固定用户和单一初始部署足以代表真实 5G-A 低空网络；
- 不把 5G-A 的覆盖、定位、身份识别或通感能力包装成本项目提出的新能力；
- 不声称 Dyna-Q 提高最终奖励，除非正式统计结果支持该结论。

后续统一叙事：面向 5G/5G-A 低空网络中的多 UAV 联合轨迹、调度与接入控制，使用
世界模型产生虚拟经验，以减少达到目标性能所需的真实环境交互；再通过 Case 2 与
Sionna RT 检查该样本效率趋势在三维遮挡、空间相关性和 LoS/NLoS 切换下是否保持。
Sionna 的作用是提供外部有效性验证，不替代当前 Rician 主实验，也不改变 Dyna-Q
只规划下层离散决策的算法边界。

目的：验证在具有空间相关性、建筑遮挡和 LoS/NLoS 切换的射线追踪信道中，
Hier-DynaQ 相对 Hier-NoDyna 的性能趋势是否保持，降低“仅适用于解析 Rician
信道”的外部有效性风险。

启动门槛：

1. 阶段 D 的 K、warm-up 和 Case 2 实验完成并归档；
2. 确定最终 Dyna 配置并冻结 checkpoint；
3. GPU 上没有其他正式训练任务；
4. Sionna 使用独立容器或虚拟环境，不修改当前稳定训练环境；
5. 载波频率、天线、材料、带宽和场景参数均有明确依据。

最小实施方案：

1. 使用 Sionna RT 构建一个开阔/郊区 LoS 场景和一个城市遮挡场景；
2. 离线预计算 GU→UAV 与 UAV→RBS 的位置—信道查找表，不在每个训练 step
   中实时执行射线追踪；
3. 明确定义从 Sionna CIR/路径增益到当前吞吐量公式所用标量信道增益的映射；
4. 首先冻结 Hier-NoDyna 和最终 Hier-DynaQ 策略，仅执行无探索、零样本泛化评估；
5. 仅当冻结策略明显失效且时间预算允许时，再考虑基于 Sionna 信道重新训练；
6. 原解析 Rician 环境保持不变，Sionna 作为独立评估后端，结果不得混入原训练统计。

#### Sionna 仿真准备包（先准备，不启动正式仿真）

执行顺序与隔离要求：

1. 当前阶段 D 正式消融结束并生成汇总 JSON 前，不安装依赖、不占用 GPU1、不修改
   服务器训练目录；
2. 记录服务器 NVIDIA 驱动、CUDA、Python、TensorFlow/Sionna 与 Mitsuba/Dr.Jit
   的兼容矩阵，选择独立 Conda 环境或容器，并冻结依赖清单与镜像摘要；
3. 先在独立目录完成一个发射端、一个接收端的自由空间 smoke test，再导入完整场景；
4. Sionna 相关代码先走 1 个位置点、1 条链路、1 个 episode 的门控验证，通过后再生成
   完整查找表；
5. 正式生成期间记录 GPU、随机种子、场景版本、材料参数、坐标系和运行命令。

建议新增但尚未创建的交付物：

| 路径 | 职责 |
|---|---|
| `environment-sionna.yml` 或独立容器文件 | 冻结 Sionna 环境，不污染主训练环境 |
| `configs/sionna_los.yaml` | LoS 场景、载频、带宽、天线、材料和射线参数 |
| `configs/sionna_urban.yaml` | 城市遮挡场景及相同口径参数 |
| `scripts/build_sionna_lut.py` | 离线生成位置—信道查找表 |
| `src/channel_backends.py` | Rician/Sionna 查找表统一接口，不复制环境逻辑 |
| `scripts/evaluate_sionna_generalization.py` | 加载冻结策略进行无探索评估 |
| `tests/test_sionna_channel_mapping.py` | 单位、维度、插值和自由空间基准测试 |
| `results/sionna/manifest.json` | 场景、版本、命令、哈希与结果文件映射 |

查找表最小数据契约：

- 坐标统一使用米，明确 Sionna 世界坐标与当前环境 `(x, y, z)` 的原点、轴向和高度映射；
- 分别保存 GU→UAV 与 UAV→RBS 链路，索引至少包含场景、发射端位置、接收端位置和
  载频；
- 主输入保存无量纲线性功率增益 `gain_linear`，禁止把 dB 值直接传入现有 Shannon
  速率公式；同时保留 `gain_db`、LoS 标记、路径数、时延扩展等诊断字段；
- 若由 CIR 计算宽带增益，先按子载波得到频域信道功率，再按当前资源块/带宽口径聚合，
  不直接对所有路径时延做无依据的标量求和；
- 查找表保存场景配置哈希、Sionna 版本、随机种子、数组 shape/dtype 和生成时间；
- 网格外位置默认报错，不静默外推；网格内插值方法在实验前冻结，并对最近邻与线性
  插值做一致性检查。

接入现有环境的边界：

- 先抽象只负责返回链路功率增益的 `ChannelBackend`，环境的能量、缓冲区、动作、奖励
  和吞吐统计保持不变；
- Rician 后端必须通过回归测试，确保引入接口前后在相同 seed 下结果一致；
- Sionna 后端仅替换 GU→UAV、UAV→RBS 的信道增益来源，不改变噪声、发射功率、带宽
  或资源块定义；若必须改变，作为新的物理场景版本单独报告；
- 不使用 Sionna 数据训练世界模型后再与旧 NoDyna checkpoint 直接比较；冻结评估与
  Sionna 重训练必须分成两个实验层级。

进入正式评估前必须通过：

1. 自由空间单路径增益与理论 Friis 结果在预设容差内；
2. dB/线性值、振幅/功率增益、距离和频率单位检查全部通过；
3. 固定场景、位置和 seed 可重复生成相同查找表；
4. LoS 场景总体强于遮挡场景，异常点能够由路径或材料记录解释；
5. Rician 后端回归测试无变化，Sionna 后端不产生 NaN、负功率或越界索引；
6. 单 episode 评估能完整输出 \(\Xi\)、交付数据量、能耗、碰撞、缓冲区和墙钟时间。

实验门控：先用同一批冻结 checkpoints 在 LoS 与城市遮挡场景各完成配对评估；若
NoDyna 与 DynaQ 均能运行且算法排序/样本效率主线可解释，再扩展到多个 seeds 或信道
随机化。若冻结策略失效，则先报告 domain shift，不立即启动昂贵重训练；只有确认
Sionna 场景与现有动作、状态、速率口径兼容后，才制定独立的 Sionna 重训练计划。

核心指标：

- 系统能效 \(\Xi\)；
- RBS 实际交付数据量和 UAV 总能耗；
- 转发/接收比、缓冲区占用和丢弃数据；
- 相对 Rician Case 1/Case 2 的性能下降比例；
- NoDyna 与 DynaQ 的算法排序是否保持；
- 离线信道生成与策略评估的墙钟成本。

建议结果矩阵：

| 评估场景 | Hier-NoDyna | 最终 Hier-DynaQ |
|---|---|---|
| Rician Case 1 | 已有/复核 | 已有/复核 |
| Rician Case 2 | 待阶段 D | 待阶段 D |
| Sionna LoS | 冻结策略评估 | 冻结策略评估 |
| Sionna 城市遮挡 | 冻结策略评估 | 冻结策略评估 |

停止条件：

- Sionna 安装或场景建模需要修改当前正式训练环境；
- 场景材料和无线参数缺乏可引用依据；
- CIR 到现有速率模型的映射无法严谨定义；
- 完成最小验证需要重跑全部主实验；
- 结果无法在 WCL 篇幅内解释且会削弱 Dyna-Q 主线。

若触发停止条件，将 Sionna 扩展转为后续 TWC/TCOM 长文或独立工作，不影响当前
WCL 投稿。

## 4. 智能体优化边界

当前优化只作用于 `HierarchicalAgent.dyna_plan()`：

- 世界模型仍只使用真实 replay transition 通过 `update_model()` 监督训练；
- 虚拟经验只更新下层 DQN；
- 上层 MADDPG、环境物理公式和 reward 定义不随本次优化改变；
- NoDyna 用相同的上层与下层结构，作为公平的无模型规划消融。

后续若要形成更强的论文创新，应优先研究“模型可信度驱动的自适应规划”，例如依据预测误差或不确定性自适应选择是否规划及 Dyna-K 值，而非仅固定 K。

## 5. IEEE WCL 投稿准备

> **WCL 准备材料已汇总至 `wcl/` 目录**：投稿最低清单、差异与贡献对照、四页 Letter
> 骨架、人工核验清单、叙事框架与四份 07-27 准备报告、外部审阅与奖励优化计划。

WCL 适合无线传感器网络、资源分配与跨层优化方向，但为严格的四页 Letter。论文应聚焦一个清晰、可验证的创新点。

### 建议的论文主张

> 提出面向 UAV 辅助 WSN 的模型驱动分层多智能体强化学习：通过解耦 UAV 轨迹—调度与 GU 模式—接入决策降低联合学习难度，并在世界模型 warm-up 后使用批量虚拟经验，提高下层策略的真实交互样本效率。

当前证据允许使用的表述：

- 5-seed 描述性结果显示分层方法相对 MADDPG 有较大的最终奖励改善趋势，但精确配对
  检验经 Holm 校正后不显著；
- Dyna-Q 平均比 NoDyna 快约 1.59 倍达到 90% 峰值奖励，但该趋势未达到显著性；
- Dyna-Q 与 NoDyna 的最终奖励和论文 Ξ 基本持平；
- Dyna-Q 墙钟训练时间更长，其样本效率趋势不等同于计算效率提升。

完成阶段 D 和更多 seeds 前禁止使用的表述：

- “Dyna-Q 显著提高样本效率或收敛速度”；
- “Dyna-Q 显著提高最终系统能效”；
- “方法在所有场景和系统规模下均优于基线”；
- “MATD3 或 CoP-MADDPG 天然不适合该问题”；
- 将本轮正奖励结果与旧版负奖励结果直接合并计算提升比例。

### 投稿前最低清单

- [x] 当前六算法首轮训练完成并归档。
- [x] 形成六算法阶段性实验报告。
- [x] 冻结用于论文实验的代码提交、环境和奖励版本。
- [x] 核心三算法至少 5 个独立随机种子。
- [x] 在同一代码提交下统一运行 MADDPG、NoDyna、Dyna-Q。
- [x] 完成奖励分解并验证 reward 与系统能效 $\Xi$ 的一致性。
- [x] 完成 Dyna-K 消融并固定 K=1。
- [x] 完成 warm-up 门控并冻结 warm-up=128（单 seed 工程选择）。
- [x] 报告能效、吞吐、能耗、碰撞与收敛速度，而非仅总 reward。
- [x] 对最终性能与样本效率分别进行显著性检验。
- [x] 解释样本效率与墙钟训练时间的差异。
- [ ] 完成至少一个 Sionna RT 射线追踪信道泛化场景（可选加分项，不阻塞核心投稿）。
- [ ] 明确与 2023 原始 HMADDPG 工作的差异和新增贡献。
- [ ] 将方法、实验设置和核心结果压缩为 4 页以内的 Letter 结构。
- [ ] 由人工核验通信物理模型、论文公式与实验主张的一致性。

## 6. 运行与维护规则

1. 训练运行期间不修改服务器 `src/` 或 `scripts/` 下的代码。
2. 任何新算法先运行短验证，再启动长训。
3. 原始奖励数据与正式结果不删除；只清理明确确认的重复 checkpoint、失败归档和重复图表。
4. 参数、算法或 reward 发生变化时，同步更新 `readme.md`、本计划和实验报告。
5. 正式实验必须记录代码提交、运行命令、随机种子和结果文件之间的映射。
6. 不跨 reward 版本、环境版本或统计口径直接计算算法提升比例。
